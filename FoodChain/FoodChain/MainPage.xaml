<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FoodChain"
             x:Class="FoodChain.MainPage">

    <StackLayout BindingContext="{StaticResource Key= chatListModels}">
        <StackLayout>
            <ListView ItemsSource="{Binding Path= ChatLists[0]}">

            </ListView>
            <Entry Text="{Binding LobbyMessage}"/>
            <Button Text="전송" Command="{Binding SendInLobby}"/>
        </StackLayout>
        <StackLayout>
            <ListView ItemsSource="{Binding Path= ChatLists[1]}">

            </ListView>
            <Entry Text="{Binding MWTNMessage}"/>
            <Button Text="전송" Command="{Binding SendInMWTN}"/>
        </StackLayout>
        

    </StackLayout>
    

</ContentPage>
